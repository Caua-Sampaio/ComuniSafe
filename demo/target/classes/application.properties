spring.application.name=ComuniSafe
spring.datasource.url=jdbc:postgresql://localhost:5432/comuniSafe
spring.datasource.username=postgres
spring.datasource.password=Q6*jMFA
spring.datasource.driver-class-name=org.postgresql.Driver

# Cria��o e atualiza��o do schema
spring.jpa.hibernate.ddl-auto=update

# Dialeto SQL do PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Mostra as queries no console (opcional)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

